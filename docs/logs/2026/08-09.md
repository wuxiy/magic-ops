# 2026-08-09 实现记忆

## 生产准入复核（独立代码级重审）

任务：回答"该项目是否具备发布到线上环境进行日常运维、运营"。在切片 30-32 关闭后，对 main 分支做代码级独立复核，不仅依据文档/计划状态。

### 验证

- `mvn test` 独立重跑：277 个测试，0 失败，12 模块全绿（与切片 32 基线一致）。
- 并行启动两个验证子代理，逐条核对差距分析的结构性主张，并以 grep/Read 交叉确认。
- 代码级确认切片 30-32 强制项真实落地（非"声明有、强制无"）：
  - `SqlGuardService`：JSQLParser 解析树分类，`TRUNCATE/DROP/ALTER/CREATE/GRANT` 恒拒绝。
  - `RepairExecutionService`：审批凭据校验 + contentHash 内容绑定 + 表级白名单 + 事务化 `MAX_AFFECTED_ROWS=100` 超限回滚。
  - `ScriptLifecycleService:167`：提交人≠审批人强制（违例 403）；actor 绑定 SecurityContext 登录用户。
  - `KeyProvider`：prod fail-fast 禁临时密钥；`PushSecretAuthenticationFilter`：收包端点 401。

### 结论

不具备上线条件。结论与 2026-08-07 差距分析一致，仅前置 P0-1/P0-2/P0-3 已由切片 30-32 关闭。硬阻断为切片 33/34 未实现：

- 切片 33（Runtime 闭环）：激活包仅内存（`PackageVerificationService:44` AtomicReference，重启即丢）；Runtime 执行审计落内存 fallback（`RuntimeApplication` 无 `@EnableJpaRepositories`/`@EntityScan`）；`HttpTargetRegistry` 生产不注册；无回滚/下线端点。
- 切片 34（执行语义）：query/adapter 仍执行请求体内容而非签名包脚本，签名包仅作"激活门票"。

其他仍开放：无 actuator/metrics/结构化日志（A5）；无 CI；集成测试全 H2；达梦方言未抽样；Docker Compose 从未真实 `docker-compose up` + PostgreSQL E2E；业务数据源/项目/环境管理只有实体无 Controller，`DynamicDataSourceManager:35` 硬编码 H2（连只读灰度都接不了业务库）。

### 修正

- 差距分析 A6"前端构建产物直接提交 git"已不成立：`.gitignore` 覆盖 `static/console/`，git 跟踪数为 0。前端未纳入 Maven 构建仍成立。

### 文档更新

- `docs/testing/known-good-baselines.md`：更新"当前基线"截止日期与测试计数（148->277），补第五阶段切片行；新增 2026-08-09 复核"最近完整验证"条目。
- `docs/analysis/2026-08-07-production-readiness-gap-analysis.md`：新增"2026-08-09 复核更新"节，标注 P0-1/2/3 已关闭并代码验证、P0-4/5 仍为硬阻断、A6 前端产物条目修正。

### 建议上线路径（不跳序）

1. 切片 33（激活包持久化 + 启动重载 + Runtime 审计 JPA 装配 + HttpTargetRegistry 同步 + 回滚/下线端点）。
2. 切片 34（执行端点脚本引用模式，需人工确认）。
3. 业务数据源/项目/环境管理 API + `DynamicDataSourceManager` 真实注册。
4. Docker Compose E2E on PostgreSQL + 达梦方言抽样。
5. 可观测 + CI（actuator/metrics/告警/结构化日志/前端纳入 Maven）。
6. 按灰度顺序上线：只读动态查询 -> HTTP 只读适配 -> 数据修复 -> Arthas 诊断。
