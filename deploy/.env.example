# deploy/ 部署凭据模板（切片 32）。复制为 .env 并填写真实值：cp .env.example .env
# .env 不入库（.gitignore 已忽略），仓库内只保留本模板。

# 平台 PostgreSQL 数据库密码（外部网络 medical-platform 的数据库容器）
DB_PASSWORD=change-me-db-password

# Console 对外映射端口（默认 18080）
MAGICOPS_PORT=18080
